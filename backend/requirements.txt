# =============================================================
#  Ask My Docs — Python Dependencies
# =============================================================

# ── API Framework ─────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9        # file uploads

# ── LLM (OpenAI) ─────────────────────────────────────────────
openai>=1.30                   # primary LLM provider (chat completions + tool-calling)

# ── Web Search ────────────────────────────────────────────────
tavily-python>=0.3             # web search fallback tool for orchestrator

# ── Vector Store ─────────────────────────────────────────────
chromadb>=0.5

# ── Embeddings, Reranking & NLI ──────────────────────────────
sentence-transformers>=3.0

# ── Sparse Retrieval ─────────────────────────────────────────
rank-bm25>=0.2.2

# ── Document Parsing ─────────────────────────────────────────
pymupdf>=1.24                  # PDF parsing (import fitz)
markdown>=3.6                  # Markdown parsing

# ── Evaluation ───────────────────────────────────────────────
ragas>=0.1.10
datasets>=2.19                 # RAGAS dataset format

# ── Config & Utilities ───────────────────────────────────────
pydantic>=2.7
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27
tiktoken>=0.7                  # token counting for fixed chunker

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                    # async test client for FastAPI
